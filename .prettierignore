node_modules
dist
build
coverage
.cache
assets
apps/server/drizzle
apps/client/public/icons
pnpm-lock.yaml

# Docs are hand-authored: Prettier reflows tables whose cells contain pipe
# characters (JSON examples), which corrupts them.
*.md

# Generated by `pnpm openapi`, which owns its formatting. Letting Prettier
# reformat it afterwards means the committed file never matches a fresh
# generation, so `pnpm openapi:check` fails on a file nobody edited.
docs/openapi
