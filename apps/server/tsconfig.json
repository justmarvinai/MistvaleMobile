{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Type checking only — esbuild produces the build output (see build.js).
    "noEmit": true,
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "drizzle.config.ts"]
}
