{
  "name": "@mistvale/sim",
  "version": "0.1.0-ea.0",
  "private": true,
  "type": "module",
  "description": "Headless stage simulation over a content bundle — pure, IO-free, shared by the CI balance gates and the Admin sandbox.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mistvale/engine": "workspace:*",
    "@mistvale/shared": "workspace:*"
  }
}
